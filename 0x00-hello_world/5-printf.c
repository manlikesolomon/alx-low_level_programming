#include <stdio.h>

/**
 * main - prints out a string via printf
 * Description: Program taht prints out a string followed by a new line
 * Return: int 0 when run with no errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
