#include <stdio.h>

/**
 * main - prints out a string via puts
 * Description: Program prints a string followed by a new line
 * Return: int 0 when there are no errors
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}	
