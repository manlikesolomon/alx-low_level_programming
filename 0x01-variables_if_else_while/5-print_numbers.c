#include <stdio.h>
/**
 * main - prints numbers from 0 to 10
 * Description: fuction prints base 10 numbers (0 to 10)
 * Return: Returns 0 for success
 */

int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}
