#include <stdio.h>
/**
 * main - prints numbers in base 10
 * Return: Returns 0 for success
 */

int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
