#include "main.h"
/**
 * mul(int a, int b) - multiplies two integers
 * @a: int type number
 * @b: int type number
 * Return: result of mulplication
 */

int mul(int a, int b)
{
	result = a * b;
	return (result);
}
