#include "main.h"
/**
 * print_last_digit - prints the last digit of an integer
 * @n: int type number
 * Return: Always 0
 */

int print_last_digit(int n);
{
	int last_digit = n % 10;

	return (last_digit);
}
