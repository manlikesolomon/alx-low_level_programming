#include <stdio.h>
/**
 * main - prints argument count
 * @argc: argument count
 * @argv: array of arguments
 * Return: 0 for success
 */

int main(__attributes__((unused)) int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
